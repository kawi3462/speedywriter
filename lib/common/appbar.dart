import 'package:flutter/material.dart';

String title;
Widget buildAppBar(String title) {
  final appBar = AppBar(
 
    title: Text(title),
    actions: <Widget>[
      
       





    ],
  );

  return appBar;
}
